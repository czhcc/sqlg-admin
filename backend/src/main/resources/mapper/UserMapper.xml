<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trs.user.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.trs.user.entity.User">
        <id     property="id"          column="id"/>
        <result property="username"    column="username"/>
        <result property="password"    column="password"/>
        <result property="nickname"    column="nickname"/>
        <result property="email"       column="email"/>
        <result property="phone"       column="phone"/>
        <result property="status"      column="status"/>
        <result property="createTime"  column="create_time"/>
        <result property="updateTime"  column="update_time"/>
    </resultMap>

    <sql id="baseColumns">
        id, username, password, nickname, email, phone, status, create_time, update_time
    </sql>

    <select id="selectByUsername" resultMap="userResultMap">
        SELECT <include refid="baseColumns"/>
        FROM sys_user
        WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="userResultMap">
        SELECT <include refid="baseColumns"/>
        FROM sys_user
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.trs.user.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, nickname, email, phone, status)
        VALUES (#{username}, #{password}, #{nickname}, #{email}, #{phone}, #{status})
    </insert>

    <update id="updatePassword">
        UPDATE sys_user
        SET password = #{password}, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
